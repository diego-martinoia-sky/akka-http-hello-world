akka {
  actor {
    serialize-messages = on
    serialize-creators = on
  }

  remote {
    log-remote-lifecycle-events = off
    netty.tcp {
      hostname = "127.0.0.1"
      port = 2551
    }
  }
}